<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>AdminPanel</title>
</head>
<body>
    
    <form method="post" action="AddData">
        <input type="text"id="pathImg" name="pathImg" placeholder="Введите url фотографии: " />
        <br />
        <br />
        <textarea id="description" name="description" placeholder="Введите новость: "></textarea>
        <br />
        <br />
        <button id="btn">Добавить новость</button>
    </form>

    <script src="~/js/jquery-3.2.1.slim.min.js"></script>
    <script>
         $(function () {
        $("#btn").on('click', function (e) {
            $.ajax
                ({
                    type: "POST",
                    contentType: "application/json; charset=utf-8",
                    url: "AddData",
                    data: "{'pathImg':'" + $('#pathImg').val() + "', 'description':'" + $('#description').val() + "'}",
                    dataType: "json",
                    success: function (msg) {
                        $("body").empty();
                        $("body").html(response.responseText);
                    },
                    error: function (response) {
                        $("body").empty();
                        $("body").html(response.responseText);
                    }
                });
            e.preventDefault();
        });

    });

    </script>
</body>
</html>